
export class Product {
    constructor(private id: number, private name: string, private description: string,
        private characteristics: string, private price: number, private stock: number,
        private mark: number) {}
}